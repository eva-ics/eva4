# System controller service
command: target/debug/eva-controller-system
bus:
  path: var/bus.ipc
config:
  report:
    oid_prefix: sensor:${system_name}/system
    cpu:
      enabled: true
    memory:
      enabled: true
    disks:
      enabled: true
      # enable specific partitions only (set partition mountpoints)
      #partitions:
        #- /
        #- /var
    network:
      enabled: true
      # enable specific interfaces only
      #interfaces:
        #- eth0
        #- eth1
user: eva
