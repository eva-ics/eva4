# Event-to-InfluxDB database service
command: target/debug/eva-db-influx
workers: 1
bus:
  path: var/bus.ipc
config:
  api_version: 1 # 1, 2 or 3 (InfluxDB 3 native write)
  ## v1 basic auth
  # username: user
  # password: pass
  ## v2 token auth
  # token: sometoken
  ## v3 token auth (Bearer)
  # token: apiv3_...
  # v1/v2 usually: http://localhost:8086
  # v3 usually: http://localhost:8181
  url: http://localhost:8086
  db: eva
  ## alternative CA file (.der for DER, any other extension for PEM)
  #tls_ca: path/to/crt
  ## required for v2
  # org: myorg
  # event buffer time-to-live, seconds
  buf_ttl_sec: null
  # periodically submit all item states
  interval: null
  # do not submit remote disconnected items (useful for zfrepl or similar)
  skip_disconnected: false
  # ignore real-time events
  ignore_events: false
  # event queue size
  queue_size: 8192
  # panic on critical errors in X seconds after happened (0 - panic immediately)
  panic_in: 0
  # influx client pool size (default = workers)
  #clients: 2
  # item OIDs / OID masks
  oids:
    - "#"
  oids_exclude: []
  # by default null values (status) are written into the database
  oids_exclude_null: []
user: nobody
